<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp">

    <!--
      教学点 1: 颜色 Selector
      注意 android:textColor 属性，它引用了 @color/button_text_selector
    -->
    <Button
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:text="按下时文本变色"
        android:textSize="20sp"
        android:textColor="@color/button_text_selector"
        android:padding="20dp"
        android:layout_marginBottom="50dp"/>

    <!--
      教学点 2: Drawable Selector
      注意 android:src 属性，它引用了 @drawable/image_button_selector

      (注意：使用 android:src，不要用 android:background)
    -->
    <ImageButton
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:src="@drawable/image_button_selector"
        android:scaleType="fitCenter"
        android:background="@android:color/transparent"
        android:padding="16dp"/>

</LinearLayout>